export const config = {
  appUrl: process.env.NEXT_PUBLIC_APP_URL || "https://app.smashscheduler.com",
  marketingUrl: process.env.NEXT_PUBLIC_MARKETING_URL || "https://smashscheduler.com",
} as const;
