export const config = {
  appUrl: process.env.NEXT_PUBLIC_APP_URL || "https://app.smashscheduler.co.uk",
  marketingUrl: process.env.NEXT_PUBLIC_MARKETING_URL || "https://smashscheduler.co.uk",
} as const;
